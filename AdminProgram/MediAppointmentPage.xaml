
<!--진료 예약 관리 페이지-->
    
<Page x:Class="AdminProgram.MediAppointmentPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:AdminProgram"
      mc:Ignorable="d" 
      d:DesignHeight="800" d:DesignWidth="1200"
      Title="MediAppointmentPage">

    <Grid Margin="60,20,20,20">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!--상단바-->
        <Grid Grid.Row="0" Grid.ColumnSpan="3" Background="DarkSlateGray">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <DatePicker x:Name="datePicker" Grid.Column="0" SelectedDateChanged="selectedDate"/>
            <Label Content="진료과: " Grid.Column="1"/>
            <TextBlock Text="내과" Grid.Column="2" VerticalAlignment="Center"/>
            <Label Content="의사이름: " Grid.Column="3"/>
            <TextBlock Text="나현정" Grid.Column="4" VerticalAlignment="Center"/>
        </Grid>
        
        <!--방문 대기 환자-->
        <Label Content="방문 대기 환자 리스트" Grid.Row="1" Grid.Column="0" 
               FontWeight="Bold" FontSize="20"/>
        <DataGrid Grid.Row="2" Grid.Column="0"
            d:ItemsSource="{d:SampleData ItemCount=10}" Background="White">
            <DataGrid.Columns>
                <DataGridTextColumn Header="환자명" Binding="{Binding PatientName}"/>
                <DataGridTextColumn Header="상태값" Binding="{Binding Statement}"/>
            </DataGrid.Columns>
        </DataGrid>

        <!--예약 환자-->
        <Label Content="예약 환자 리스트" Grid.Row="1"  Grid.Column="2" 
               FontWeight="Bold" FontSize="20"/>
        <DataGrid Grid.Row="2" Grid.Column="2"
                  d:ItemsSource="{d:SampleData ItemCount=10}" Background="White">
            <DataGrid.Columns>
                <DataGridTextColumn Header="예약시간" Binding="{Binding ReserveTime}"/>
                <DataGridTextColumn Header="환자명" Binding="{Binding PatientName}"/>
                <DataGridTextColumn Header="상태값" Binding="{Binding Statement}"/>
            </DataGrid.Columns>
        </DataGrid>

        <!--진료 완료-->
        <Label Content="진료 완료 리스트" Grid.ColumnSpan="3" Grid.Row="3" 
               FontWeight="Bold" FontSize="20" Margin="0,10,0,10"/>
        <DataGrid Grid.ColumnSpan="3" Grid.Row="4" 
                  d:ItemsSource="{d:SampleData ItemCount=10}" Background="White">
            <DataGrid.Columns>
                <DataGridTextColumn Header="환자명" Binding="{Binding PatientName}"/>
                <DataGridTextColumn Header="상태값" Binding="{Binding Statement}"/>
            </DataGrid.Columns>
        </DataGrid>

    </Grid>
</Page>
